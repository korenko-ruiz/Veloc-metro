{
  "name": "veloci-gps",
  "version": "1.0.0",
  "private": true,
  "description": "Velocímetro GPS con mapa, historial de viajes, alerta de velocidad y reproductor de música.",
  "scripts": {
    "android:open": "npx cap open android",
    "android:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.0",
    "@capacitor/core": "^6.1.0",
    "@capacitor/geolocation": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.0"
  }
}
